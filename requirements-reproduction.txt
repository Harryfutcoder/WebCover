# Python 3.11. Install torch from the CPU index first (see docs/REPRODUCING.md).
# These are actual import versions observed in the uniform-UAA campaign,
# not misleading metadata from shadowed Conda packages.
torch==2.6.0
numpy==1.26.4
selenium==4.22.0
PyYAML==6.0.3
urllib3==2.4.0
scikit-learn==1.5.2
translate==3.6.1
beautifulsoup4==4.12.3
gensim==4.3.3
scipy==1.13.1
pytest==9.1.1
